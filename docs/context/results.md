# Results

- 2026-05-27: RoRo engine implementation plan executed end-to-end.
- All numeric kernels covered by pytest unit + hypothesis property tests.
- Reproducibility invariant enforced: two runs over same fingerprint produce byte-identical CSVs.
- Golden integration test pinned at tests/golden/2024-Q1/.
- Full suite green: pytest, mypy --strict, ruff check.
- Engine produces beta_series.csv, regimes.csv, correlation.csv, external_validation.csv, alerts.csv, tripwire.csv, snapshot.json per run.
- Acceptance gates (G1–G6) wired in backtest harness, evaluated via `roro backtest --assert-gates`.
- 2026-06-03 Task 4: roro/regime_hmm.py created — _fit_params uses model.param_names (not Series index) since statsmodels 0.14.x returns plain ndarray; 2 tests green, mypy strict clean, ruff clean.
- 2026-06-09 Task 5: diagnose() + write_diagnostics() added to gate_diagnostics.py. 2 integration tests (gate-tag coverage + write determinism). 187 tests green, mypy --strict + ruff clean. Commit f78715e.
- 2026-06-03 T12: _evaluate_gates parametrized over explicit labels/transitions; run_backtest scores both percentile + HMM through shared G1–G6 gates; writes acceptance_report_hmm.json + acceptance_compare.json when HMM enabled; fast+slow tests green, mypy+ruff clean.
- 2026-06-03 T14: HMM cadence bench: monthly(21) vs daily(1) refit label agreement = 0.9966 on a 501-day 3-regime series → monthly cadence validated as ~equivalent to daily (slow bench runtime ~7m47s: tests/test_regime_hmm_cadence.py).
- 2026-06-03 T15: real-data HMM-vs-percentile backtest (2008–2026, quarterly refit, outputs/hmm_eval/). Neither passes all 6 gates. G3 events: percentile 7/8 vs HMM 6/8 (HMM lost 2010 Greek). G5 stability: HMM 9 vs percentile 18 transitions/calm-qtr (HMM halves flicker — thesis validated directionally). G4: 0.183 vs 0.161. G1/G2 shared (method-agnostic in v1.1). DECISION (§8): percentile stays production default; HMM ships off-by-default overlay (hmm_enabled=False). HMM regressed G3 + passes no gate → cannot be default.
- 2026-06-03 NOTE: percentile baseline fails 5/6 gates on full 2008–2026 data (G1 0.507, G2 0.682 vs 0.80; G5 18 vs ≤2). Pre-existing calibration gap — "S9 done" = harness built, not gates green on real data. Revisit gate thresholds / params.
- 2026-06-09 Task 4 (gate-diagnostics): g3_g5_frontier implemented in roro/gate_diagnostics.py — causal G3xG5 tradeoff frontier (events caught vs worst calm-quarter transitions vs confirm_days). TDD: 3 tests (monotonicity, determinism, no-lookahead) all green. 8/8 total diagnostic tests pass. mypy --strict + ruff clean. Committed f5b469a.
- 2026-06-09 Task 6 (cli): `roro gate-diagnostics` subcommand added to roro/cli.py. TDD smoke test green. 188 tests pass, 7 pre-existing mypy errors in test_regime_hmm.py/test_figures.py (not introduced here), ruff clean. Commit c5c1892.
- 2026-06-09 Task 7 (memo): gate-diagnostics harness run (2008-12-31 → 2026-05-26), baseline_pinned=true. Headline: only G5 is a real discriminating failure (18 vs ≤2); G1/G2 method-shared, G3 bug (2008_lehman pre-dates backtest start), G4 borderline (0.183 vs 0.20), G6 vacuous. G3↔G5 frontier: no confirm_days setting on the percentile classifier achieves 7/7 in-range events AND ≤2 calm transitions simultaneously — that joint bar is the Statistical Jump Model's success target. Memo: docs/analysis/2026-06-09-gate-diagnostics-memo.md.
- 2026-06-09 Jump Model (feat/jump-model, J0–J6 subagent-driven TDD + per-task senior review): vendored numpy-only deterministic JM. roro/jump_model.py (seeded k-means++, O(T·K²) forward/Viterbi DP, coordinate descent, empty-cluster sequential reseed, n_init restarts, mean-canonical labels; discrete + continuous simplex-grid). roro/regime_jm.py (causal walk_forward: expanding/rolling refit + per-block frozen-scaler forward-DP online inference; classify_jm per-segment → JmRegimeFrame). Wired config(jm_* 12 fields)/types/engine/io(regimes_jm.csv+jm_refit_log.csv+snapshot)/alerts/backtest(3-way acceptance_compare). Report: 3-way Percentile/HMM/JM band toggle + JM probability figure (fig_jm_probs). Verified: AJ-1 off=byte-identical, AJ-2 byte-identical regimes_jm.csv for BOTH discrete & continuous at engine level, AJ-3 no-lookahead (discrete+CJM, incl. block boundary), λ=0==k-means identity. Full non-slow suite green (224), mypy --strict + ruff clean. Off by default; promotion deferred to AJ-7.
