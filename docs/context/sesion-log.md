# Session log

- 2026-05-27: brainstorm → design spec → implementation plan → engine v0.1.0 built (23 tasks, subagent-driven).
- 2026-05-27: viz layer landed — roro.report package + roro report CLI. 3 Plotly figures (vol×ret scatter, β×ret scatter, β time-series with tercile bands) in single self-contained HTML.
- 2026-05-28: viz v2 — 7 segments (added DM_Eq, DM_FI), EM recolored green (#2ca02c), 95% CI ribbons via scipy.linregress, seaborn-like aesthetic, height=700, dropdown clear of legend.
- 2026-05-28: viz v3 — per-segment scatter axis ranges, legend top-left overlay, slider clear of axis title, beta time-series now spans full history, hover behavior locked with a test.
- 2026-05-28: viz v4 — regime bands legible: 21-day hysteresis smoothing collapses daily tercile flicker into broad blocks; opacity raised to 0.28/0.15/0.28. Cosmetic only; engine regimes.csv unchanged.
- 2026-06-03: pivot — defer S8 dashboard. Brainstormed HMM/Markov-switching regime classifier (3-state, β mean+var switching, statsmodels, filtered+point-in-time monthly refit, coexist with percentile). Spec written: docs/superpowers/specs/2026-06-03-roro-hmm-regime-design.md. Branch feat/hmm-regime.
- 2026-06-03: Task 4 — roro/regime_hmm.py + tests/test_regime_hmm.py committed (9130a23). TDD green; discovered statsmodels 0.14.x res.params is ndarray not Series — extraction uses model.param_names.
- 2026-06-03: HMM build complete (T1–T15, subagent-driven, 24 commits, branch feat/hmm-regime). Causality + cadence(0.9966) verified. T15 real-data backtest → percentile stays default, HMM off-by-default overlay (no gate passed, G3 regressed, G5 flicker halved 18→9). Suite green, mypy+ruff clean.
- 2026-06-05: report v1.1 merged to main (squash of feat/hmm-report-viz, 19 commits, subagent-driven). HMM viz (stacked filtered-prob area + percentile<->HMM band toggle) + vol-breadth heatmaps (sorted-rank + per-asset, All/Eq/FI toggle, Plasma, expanding-min-5Y vol percentile, warmup-trimmed). assemble refactored to FigureSpec. Suite green, mypy+ruff clean.
- 2026-06-09: Task 4 gate-diagnostics — g3_g5_frontier implemented (f5b469a). TDD: 3 tests (monotone, deterministic, no-lookahead) + 5 prior tests = 8/8 green. mypy --strict + ruff clean. Reuses all 4 production primitives verbatim.
- 2026-06-09: Task 5 gate-diagnostics — diagnose() orchestrator + write_diagnostics() + baseline pin committed (f78715e). 10/10 gate_diagnostics tests green, 187 total. mypy --strict + ruff clean.
- 2026-06-09: Task 6 — `roro gate-diagnostics` CLI subcommand TDD (c5c1892). Smoke test + 188 total passing. Lazy-imports gate_diagnostics inside command; re-runs engine for fresh RunResult. ruff clean; 7 pre-existing mypy errors in test files not introduced here.
- [2026-06-09]: gate-diagnostics harness run (2008-2026) + findings memo; only G5 is a real gate failure; G3<->G5 frontier sets the JM bar. JM core (jump_model.py) built next.
- [2026-06-09]: Statistical Jump Model built end-to-end (branch feat/jump-model, subagent-driven TDD + senior review per task). Research workflow (6 agents, exact SJM math + D4 vendor verdict) -> spec (senior-reviewed) -> plan (J0-J6) -> impl. Vendored numpy-only deterministic JM (jump_model.py: seeded k-means++ + O(T K^2) Viterbi DP + coordinate descent; regime_jm.py: causal walk_forward + classify_jm). Wired through config/types/engine/io/alerts/backtest + report (3-way band toggle + JM prob figure). AJ-1/AJ-2(discrete+CJM)/AJ-3 verified; full non-slow suite green; mypy --strict + ruff clean. Off by default; promotion deferred to AJ-7 decision.
